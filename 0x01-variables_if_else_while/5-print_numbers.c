#include "stdio.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int Count;

	for (Count = 0; Count <= 9; Count++)
	{
		printf("%d", Count);
	}

	printf("\n");


	return (0);
}
