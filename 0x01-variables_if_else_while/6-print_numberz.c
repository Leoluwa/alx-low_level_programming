#include "stdio.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	short Count;

	for (Count = 48; Count <= 57; Count++)
	{
		putchar(Count);
	}

	putchar('\n');


	return (0);
}
